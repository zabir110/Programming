#include <iostream>

using namespace std;

int main()
{
    string s,a;
    while(getline(cin,s))
    {
        for(int i=0; i<s.length(); i++)
        {
            if((s[i]=='[' && s[i+1]!=']')||m==1)
            {
                s[i]=' ';
                a+=s[i];
                m=1;
            }
            else if( )
            {


            }
        }
    }
    return 0;
}
