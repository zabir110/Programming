#include <bits/stdc++.h>

using namespace std;

map <int,vector<int> > m;

int main()
{
    for(int i=0;i<5;++i)
    {
        m[i].push_back(i);
    }
    ///for(auto iterator it=m.begin();it!=m.end();++it) cout<<m[i];
    for(int i=0;i<5;++i)
    {
        for(auto iterator it=i->)
    }
    return 0;
}
